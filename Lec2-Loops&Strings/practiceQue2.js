// //**** Practice Questions ****//
/* Que1. Prompt the user to enter their full name. Generate a username for them based on the input. Start username with @.
followed by their full name and ending with the fullname length.
rg: user = MaheshSutar, username should be "@MaheshSutar" */

let username = prompt("Enter your full name without space:");
console.log("Username is :","@" + username + username.length);